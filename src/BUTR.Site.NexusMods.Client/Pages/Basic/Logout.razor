@attribute [Authorize]
@page "/logout"

@inject AuthenticationProvider _authentication

@code {

    protected override async Task OnInitializedAsync()
    {
        await _authentication.DeauthenticateAsync();
    }

}